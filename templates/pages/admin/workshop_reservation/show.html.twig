{% extends 'base.html.twig' %}

{% block title %}WorkshopReservation{% endblock %}

{% block body %}
    <h1>WorkshopReservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workshop_reservation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_workshop_reservation_index') }}">back to list</a>

    <a href="{{ path('admin_workshop_reservation_edit', {'id': workshop_reservation.id}) }}">edit</a>

    {{ include('pages/admin/workshop_reservation/_delete_form.html.twig') }}
{% endblock %}
