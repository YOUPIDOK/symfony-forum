{% extends 'base.html.twig' %}

{% block title %}Edit workshopReservation{% endblock %}

{% block body %}
    <div class="p-4">
        <div class="flex justify-between">
            <h1>Modifier une reservation d'atelier</h1>
            {{ include('pages/admin/workshop_reservation/_delete_form.html.twig') }}
        </div>

        <div class="my-4">
            {{ include('pages/admin/workshop_reservation/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <a class="inline-block text-slate-600 hover:text-slate-900 mt-4" href="{{ path('admin_workshop_reservation_index') }}">back to list</a>
    </div>
{% endblock %}
