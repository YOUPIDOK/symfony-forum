{% extends 'base.html.twig' %}

{% block title %}JobSkill{% endblock %}

{% block body %}
    <h1>JobSkill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job_skill.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_job_skill_index') }}">back to list</a>

    <a href="{{ path('admin_job_skill_edit', {'id': job_skill.id}) }}">edit</a>

    {{ include('pages/admin/job_skill/_delete_form.html.twig') }}
{% endblock %}
