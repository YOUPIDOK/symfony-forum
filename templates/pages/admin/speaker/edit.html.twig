{% extends 'base.html.twig' %}

{% block title %}Edit Speaker{% endblock %}

{% block body %}
    <div class="p-4">
        <div class="flex justify-between">
            <h1>Modifier un intervenant</h1>
            {{ include('pages/admin/speaker/_delete_form.html.twig') }}
        </div>

        <div class="my-4">
            {{ include('pages/admin/speaker/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <a class="inline-block text-slate-600 hover:text-slate-900 mt-4" href="{{ path('admin_speaker_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
