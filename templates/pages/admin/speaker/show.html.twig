{% extends 'base.html.twig' %}

{% block title %}Speaker{% endblock %}

{% block body %}
    <h1>Speaker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ speaker.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ speaker.createdAt ? speaker.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ speaker.updatedAt ? speaker.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_speaker_index') }}">back to list</a>

    <a href="{{ path('admin_speaker_edit', {'id': speaker.id}) }}">edit</a>

    {{ include('pages/admin/speaker/_delete_form.html.twig') }}
{% endblock %}
