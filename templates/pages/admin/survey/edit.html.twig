{% extends 'base.html.twig' %}

{% block title %}Edit Survey{% endblock %}

{% block body %}
    <div class="p-4">
        <div class="flex justify-between">
            <h1>Modifier un étudiant</h1>
            {{ include('pages/admin/survey/_delete_form.html.twig') }}
        </div>

        <div class="my-4">
            {{ include('pages/admin/survey/_form.html.twig', {'button_label': 'modifier'}) }}
        </div>

        <a class="inline-block text-slate-600 hover:text-slate-900 mt-4" href="{{ path('admin_survey_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
