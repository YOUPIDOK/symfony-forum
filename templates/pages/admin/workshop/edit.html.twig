{% extends 'base.html.twig' %}

{% block title %}Edit Workshop{% endblock %}

{% block body %}
    <div class="p-4">
        <div class="flex justify-between">
            <h1>Modifier un atelier</h1>
            {{ include('pages/admin/workshop/_delete_form.html.twig') }}
        </div>

        <div class="my-4">
            {{ include('pages/admin/workshop/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <a class="inline-block text-slate-600 hover:text-slate-900 mt-4" href="{{ path('admin_workshop_index') }}">back to list</a>
    </div>
{% endblock %}
