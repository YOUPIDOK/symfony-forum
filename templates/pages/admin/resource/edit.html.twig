{% extends 'base.html.twig' %}

{% block title %}Edit Resource{% endblock %}

{% block body %}
    <div class="p-4">
        <div class="flex justify-between">
            <h1>Modifier une ressource</h1>
            {{ include('pages/admin/resource/_delete_form.html.twig') }}
        </div>

        <div class="my-4">
            {{ include('pages/admin/resource/_form.html.twig', {'button_label': 'modifier'}) }}
        </div>

        <a class="inline-block text-slate-600 hover:text-slate-900 mt-4" href="{{ path('admin_resource_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
