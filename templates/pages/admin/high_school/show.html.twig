{% extends 'base.html.twig' %}

{% block title %}HighSchool{% endblock %}

{% block body %}
    <h1>HighSchool</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ high_school.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ high_school.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_high_school_index') }}">back to list</a>

    <a href="{{ path('admin_high_school_edit', {'id': high_school.id}) }}">edit</a>

    {{ include('pages/admin/high_school/_delete_form.html.twig') }}
{% endblock %}
