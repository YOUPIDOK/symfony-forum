{% extends 'base.html.twig' %}

{% block title %}Edit HighSchool{% endblock %}

{% block body %}
    <div class="p-4">
        <div class="flex justify-between">
            <h1>Modifier un Lycée</h1>
            {{ include('pages/admin/high_school/_delete_form.html.twig') }}
        </div>

        <div class="my-4">
            {{ include('pages/admin/high_school/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <a class="inline-block text-slate-600 hover:text-slate-900 mt-4" href="{{ path('admin_high_school_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
