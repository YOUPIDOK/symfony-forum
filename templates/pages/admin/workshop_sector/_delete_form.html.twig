<form method="post" action="{{ path('admin_workshop_sector_delete', {'id': workshop_sector.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workshop_sector.id) }}">
    <button class="btn">Delete</button>
</form>
