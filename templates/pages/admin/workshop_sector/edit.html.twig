{% extends 'base.html.twig' %}

{% block title %}Edit WorkshopSector{% endblock %}

{% block body %}
    <div class="p-4">
        <div class="flex justify-between">
            <h1>Modifier un secteur</h1>
            {{ include('pages/admin/workshop_sector/_delete_form.html.twig') }}
        </div>

        <div class="my-4">
            {{ include('pages/admin/workshop_sector/_form.html.twig', {'button_label': 'modifier'}) }}
        </div>

        <a class="inline-block text-slate-600 hover:text-slate-900 mt-4" href="{{ path('admin_workshop_sector_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
