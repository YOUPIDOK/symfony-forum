{% extends 'base.html.twig' %}

{% block title %}WorkshopSector{% endblock %}

{% block body %}
    <h1>WorkshopSector</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workshop_sector.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ workshop_sector.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ workshop_sector.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_workshop_sector_index') }}">back to list</a>

    <a href="{{ path('admin_workshop_sector_edit', {'id': workshop_sector.id}) }}">edit</a>

    {{ include('pages/admin/workshop_sector/_delete_form.html.twig') }}
{% endblock %}
