{% extends 'base.html.twig' %}

{% block title %}Edit Student{% endblock %}

{% block body %}
    <div class="p-4">
        <div class="flex justify-between">
            <h1>Modifier un étudiant</h1>
            {{ include('pages/admin/student/_delete_form.html.twig') }}
        </div>

        <div class="my-4">
            {{ include('pages/admin/student/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <a class="inline-block text-slate-600 hover:text-slate-900 mt-4" href="{{ path('admin_student_index') }}">back to list</a>
    </div>
{% endblock %}
