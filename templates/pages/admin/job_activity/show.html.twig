{% extends 'base.html.twig' %}

{% block title %}JobActivity{% endblock %}

{% block body %}
    <h1>JobActivity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_activity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job_activity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_job_activity_index') }}">back to list</a>

    <a href="{{ path('admin_job_activity_edit', {'id': job_activity.id}) }}">edit</a>

    {{ include('pages/admin/job_activity/_delete_form.html.twig') }}
{% endblock %}
