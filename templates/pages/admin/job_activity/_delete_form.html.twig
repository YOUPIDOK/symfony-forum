<form method="post" action="{{ path('admin_job_activity_delete', {'id': job_activity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_activity.id) }}">
    <button class="btn">Delete</button>
</form>
